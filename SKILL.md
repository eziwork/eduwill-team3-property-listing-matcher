---
name: eduwill-property-listing-matcher
description: >-
  공인중개사의 매물장을 Google Sheets 또는 로컬 Excel(.xlsx)로 만들거나 연결하고, 매물을 추가·수정·완료 처리하며,
  손님 조건으로 검색해 검증된 후보와 확인 필요 후보를 구분한다. 사용자가 매물장 생성·연결·전환, 매물 등록·변경,
  조건 검색, 손님용 매물 안내를 요청할 때 사용한다. 시세·광고 문구·계약서·법령·세금만 요청하면 사용하지 않는다.
---

# 매물 매칭

운영 원본은 Google Sheets 또는 로컬 Excel 하나다. 요청을 분류하고 원본 연결을 확인한 뒤 작업한다.

## 설치 요청

이 스킬 ZIP의 설치를 요청받으면 스킬 파일을 Codex 스킬 경로에 배치하고 설치 완료만 알린다. 설치 과정에서는 번들 테스트,
`quick_validate`, 환경·의존성 점검, `doctor`, 플러그인 확인, 프로필 조회, 설치 후 감사를 실행하지 않는다. 사용자가 별도로
요청한 경우에만 해당 검사를 수행한다. 이 규칙은 설치에만 적용하며, 설치 후 실제 매물장 업무를 시작할 때는 아래 환경 확인과
연결 게이트를 따른다.

## 환경 확인

첫 사용·도움말 또는 이 환경에서 처음 로컬 도구를 실행할 때 `references/environment-setup.md`를 읽는다. 운영체제를 확인하고
Python 3.10+와 필요한 경우 `openpyxl`을 검사한다. 없는 항목은 macOS와 Windows에 맞는 설치법을 보여주며, 사용자의 확인 없이
시스템 소프트웨어나 패키지를 설치하지 않는다. Google 기능은 사용자가 Google을 선택했을 때만 플러그인·로그인·권한을 검사한다.

## 시작·등록 안내

1. 첫 사용·도움말·등록 여부·활성 매물장 질문 또는 활성 프로필이 없을 때만 `references/getting-started.md`를 읽고
   읽기 전용 현황과 짧은 안내를 제공한다.
2. 등록 방법을 묻고 매물 정보는 아직 주지 않았다면 같은 문서의 등록 안내를 사용한다. 이미 정보를 줬다면 안내를
   반복하지 말고 활성 원본과 정리한 값을 보여준 뒤 저장 확인으로 진행한다.
3. 활성 프로필이 있고 추가·수정·검색처럼 구체적인 요청이면 소개와 예시를 반복하지 않는다.

`profile list`의 수는 **매물장 연결 프로필 수**다. 실제 원본을 읽어 집계한 `매물` 행 수와 섞어 말하지 않는다.

## 요청 분류

1. **새 매물장 생성**: 사용자가 지정한 방식이 활성 프로필보다 우선한다. Excel이면 Google 도구를 호출하지 않고,
   폴더가 없을 때만 한 번 묻는다. Google이면 Google 연결 문서의 게이트를 거쳐 생성한다.
2. **기존 연결 관리**: 전환은 `profile activate`, 같은 방식의 원본 재연결은 `profile reconnect`를 사용한다.
   연결 해제는 원본을 삭제하지 않지만 반드시 대상 프로필과 원본을 보여주고 명시적으로 확인받은 뒤 `profile disconnect`를
   사용한다. 저장 방식 변경은 새 프로필로 연결한다.
3. **현재 매물장 작업**: `profile show`로 활성 프로필을 확인하고 선택한 저장 방식 문서의 연결 게이트를 실행한다.

프로필이 없고 방식도 말하지 않았다면 `references/setup.md`에 따라 Google Sheets와 Excel 중 하나만 선택받는다.

## 연결 게이트

- Google Sheets는 `references/google-sheets-workflow.md`의 도구·계정·ID·탭·헤더 검사를 모두 통과해야 한다.
  실패 시 Excel로 자동 전환하지 않는다.
- 로컬 Excel은 저장된 절대경로의 파일·두 탭·헤더를 검증한다. 이동·삭제·헤더 오류 시 빈 파일을 만들지 말고
  정확한 새 경로를 받아 `profile reconnect`한다.

## 안전 불변식

- 검색은 읽기 전용이다. 변경은 활성 원본, 대상, 전후 값을 확인받은 뒤 수행한다.
- 추가 전 번호·중복 후보를 검사하고, 쓰기 후 해당 셀을 재조회한다. 확인 생략 요청도 검증은 생략하지 않는다.
- 원본 파일·시트·행은 삭제하지 않는다. 프로필 연결 해제도 원본이나 매물 데이터를 변경하지 않는다.
- 종료는 `상태=완료`, 일시 제외는 `상태=보류`다.
- 모르는 값은 `?`, 해당 없는 가격은 빈칸이다. `종류·거래·지역` 중 하나라도 미확인이면 `상태=보류`로 강제한다.
- 값이 채워져도 자동으로 진행으로 바꾸지 않는다. 사용자가 명시적으로 요청할 때만 진행 처리한다.
- 개인정보는 `매물상세`에만 둔다. 상세 번호는 같은 원본의 `매물` 번호에 반드시 존재해야 한다.
- Google은 대상 범위만 배치 변경하고 재조회한다. Excel은 해시·백업·원자 교체와 셀 단위 변경을 사용한다.
- `needs_verification`은 적합 건수에 포함하지 않고 요청 시에만 별도 후보로 보여준다.
- 번호 없는 수정·완료 요청에서 후보가 둘 이상이면 쓰지 말고 선택받는다.

## 응답 계약

- 쓰기 전: 운영 원본, 대상 번호, 바뀌는 값만 보여주고 확인받는다.
- 성공: 처리 건수·번호·실제 변경값·경고·원본 링크 또는 파일·다음 행동 하나를 말한다.
- 실패: 실패 단계, 원본 미변경 여부, 다음 행동 하나를 말한다.
- 부분 성공: 재조회로 확인한 반영·미반영 번호를 나누고 새 번호로 중복 생성하지 않는다.
- 내부 명령·JSON·셀 범위보다 업무 결과를 먼저 말한다.

중복 후보가 있으면 저장 사실과 기존 번호를 알리고 `둘 다 유지`, `두 번호 비교`, `하나를 보류` 중 하나를 고르게 한다.

## 작업별 문서

| 작업 | 읽을 문서 |
| --- | --- |
| 첫 환경 점검·설치 문제 | `references/environment-setup.md` |
| 시작·등록 안내·활성 현황 | `references/getting-started.md` |
| 최초 설정·새 매물장·연결 관리 | `references/setup.md` + `references/tool-usage.md` + 선택한 저장 방식 문서 |
| Google 운영 | `references/google-sheets-workflow.md` |
| Excel 운영 | `references/excel-workflow.md` |
| 조건 검색·정렬 | `references/matching-rules.md` |
| 손님용 문안 | `references/client-message.md` |
| 자료 이관 | `references/import-workflow.md` + 선택한 저장 방식 문서 |
| 헤더·값 규칙 | `references/sheet-schema.md` |

한 요청에서 필요한 문서만 읽는다. 검색 `warnings`는 전달하고, 거래유형이 둘 이상이면 유형별로 나눠 유형 간 가격 순위를
만들지 않는다.
